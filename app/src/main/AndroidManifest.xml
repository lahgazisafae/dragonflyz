<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.northampton.smith.safae.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/travel_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <activity android:name=".TripDetailsActivity" />
        </activity>
        <activity android:name=".Trips_SQLite" />
        <activity android:name=".TripDetailsActivity" />
        <activity android:name=".DiaryEntryActivity"></activity>
    </application>

</manifest>